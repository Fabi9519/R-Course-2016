q()
library(XML)
install.packages(XML)
install.packages('XML')
library(XML)
tokens <- vector('character')
types <- vector('character')
wortart <- vector('character')
xmlEventParse(
"../data/t_990505_47.xml",
handlers = list(
't' = function(name, attr) {
tokens <<- c(tokens, attr['word'])
types <<- c(types, attr['lemma'])
wortart <<- c(wortart, atrr['pos'])
## morphology
}
),
addContext = FALSE
)
library(XML)
library(XML)
tokens <- vector('character')
types <- vector('character')
wortart <- vector('character')
xmlEventParse(
"../data/t_990505_47.xml",
handlers = list(
't' = function(name, attr) {
tokens <<- c(tokens, attr['word'])
types <<- c(types, attr['lemma'])
wortart <<- c(wortart, atrr['pos'])
## morphology
}
),
addContext = FALSE
)
library(XML)
tokens <- vector('character')
types <- vector('character')
wortart <- vector('character')
xmlEventParse(
"../data/t_990505_47.xml",
handlers = list(
't' = function(name, attr) {
tokens <<- c(tokens, attr['word'])
types <<- c(types, attr['lemma'])
wortart <<- c(wortart, attr['pos'])
## morphology
}
),
addContext = FALSE
)
#names(tokens) <- NULL
tokens <- unname(tokens)
df1 <- data.frame(tokens, types, wortart)
df1[order(df1$), ]
df1[order(df1$tokens),  ]
mean(subset(df1$wortart == 'NN' & df1[length(df1$wortart), ]))
mean(subset(df1$wortart == 'NN'))
set.seed(1)
df1[sample(length(df1$wortart)), ]
-df1[order(df1$tokens),  ]
library(XML)
tokens <- vector('character')
types <- vector('character')
wortart <- vector('character')
xmlEventParse(
"../data/t_990505_47.xml",
handlers = list(
't' = function(name, attr) {
tokens <<- c(tokens, attr['word'])
types <<- c(types, attr['lemma'])
wortart <<- c(wortart, attr['pos'])
## morphology
}
),
addContext = FALSE
)
#names(tokens) <- NULL
tokens <- unname(tokens)
library(XML)
tokens <- vector('character')
types <- vector('character')
wortart <- vector('character')
xmlEventParse(
"../data/t_990505_47.xml",
handlers = list(
't' = function(name, attr) {
tokens <<- c(tokens, attr['word'])
types <<- c(types, attr['lemma'])
wortart <<- c(wortart, attr['pos'])
## morphology
}
),
addContext = FALSE
)
#names(tokens) <- NULL
tokens <- unname(tokens)
df1 <- data.frame(tokens, types, wortart)
df1[order(df1$tokens),  ]
nchar(tokens)
mean(subset(nchar(tokens), df1$wortart == 'NN'))
library(XML)
tokens <- vector('character')
types <- vector('character')
wortart <- vector('character')
genus <- vector('character')
xmlEventParse(
"../data/t_990505_47.xml",
handlers = list(
't' = function(name, attr) {
tokens <<- c(tokens, attr['word'])
types <<- c(types, attr['lemma'])
wortart <<- c(wortart, attr['pos'])
genus <<- c(genus, attr['morph'])
## morphology
}
),
addContext = FALSE
)
#names(tokens) <- NULL
tokens <- unname(tokens)
df1
library(XML)
tokens <- vector('character')
types <- vector('character')
wortart <- vector('character')
genus <- vector('character')
xmlEventParse(
"../data/t_990505_47.xml",
handlers = list(
't' = function(name, attr) {
tokens <<- c(tokens, attr['word'])
types <<- c(types, attr['lemma'])
wortart <<- c(wortart, attr['pos'])
genus <<- c(genus, attr['morph'])
## morphology
}
),
addContext = FALSE
)
#names(tokens) <- NULL
tokens <- unname(tokens)
df1
library(XML)
tokens <- vector('character')
types <- vector('character')
wortart <- vector('character')
genus <- vector('character')
xmlEventParse(
"../data/t_990505_47.xml",
handlers = list(
't' = function(name, attr) {
tokens <<- c(tokens, attr['word'])
types <<- c(types, attr['lemma'])
wortart <<- c(wortart, attr['pos'])
genus <<- c(genus, attr['morph'])
## morphology
}
),
addContext = FALSE
)
#names(tokens) <- NULL
tokens <- unname(tokens)
df1
library(XML)
tokens <- vector('character')
types <- vector('character')
wortart <- vector('character')
genus <- vector('character')
xmlEventParse(
"../data/t_990505_47.xml",
handlers = list(
't' = function(name, attr) {
tokens <<- c(tokens, attr['word'])
types <<- c(types, attr['lemma'])
wortart <<- c(wortart, attr['pos'])
genus <<- c(genus, attr['morph'])
}
),
addContext = FALSE
)
#names(tokens) <- NULL
tokens <- unname(tokens)
df
df1
df1 <- data.frame(types, tokens, wortart, genus)
df1
df1[order(df1$genus),  ]
dnorm(morph, mean = 0, sd = 1, log = TRUE)
table(df1$genus)
sort(table(df1$genus))
"%in%" <- c(tokens, df1) match(types, df1, match = 1) > 0
types %in% tokens
table(tokens %in% types)
table(df1$tokens %in% df1$types)
table (df1types %in% df1$tokens)
table (df1$types %in% df1$tokens)
write.table(df1, file ='df1.csv', sep='\t')
ls
setwd("C:/Users/Fabienne/Desktop/R-Course-2016/Session_05")
write.table(df1, file ='df1.csv', sep='\t')
